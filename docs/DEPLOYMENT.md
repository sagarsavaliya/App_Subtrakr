# Deployment

SubTrakr's backend is self-hosted Supabase (Postgres + Auth + REST + Storage
via Kong) on a Hostinger VPS, alongside several unrelated projects. CI/CD runs
on a dedicated self-hosted GitHub Actions runner installed on that same VPS
(service `actions.runner.sagarsavaliya-App_Subtrakr.subtrakr-vps`, labeled
`subtrakr`, running as the unprivileged `runner` user — **not** root).

## Layout on the VPS

- Compose project: `/var/www/subtrakr-supabase/` — `.env` (real secrets,
  never committed), `docker-compose.yml`, `kong.yml` (generated from the
  template below), `schema.sql`.
- Shared reverse proxy: `nginx-proxy` (hand-configured `nginx:alpine`, used by
  every project on this box — **not** an auto-discovery proxy). Config for
  this project lives at `/var/www/nginx-proxy/conf.d/subtrakr-supabase.conf`.
- Docker network: `subtrakr_net`, isolated to this project's containers
  (`subtrakr_db`, `subtrakr_auth`, `subtrakr_rest`, `subtrakr_storage`,
  `subtrakr_meta`, `subtrakr_kong`) — `nginx-proxy` is additionally connected
  to this network so it can reach Kong by container name.

## What's in this repo vs. what's VPS-only

| In git (`backend/`) | VPS-only, never committed |
|---|---|
| `docker-compose.yml` | `.env` (Postgres password, JWT secret, anon/service_role keys) |
| `kong.yml.template` (placeholders) | `kong.yml` (real keys, generated by CD via `envsubst`) |
| `schema.sql` — subscription-tracking tables (entities, subscriptions, payment_history, invoices) | — |
| `schema_billing.sql` — SubTrakr's *own* billing (plans, subscriber_billing, billing_transactions, admin_users, app_settings) | Razorpay API keys, SMTP creds, etc. — stored **in the `app_settings` table**, encrypted with `pgcrypto`, managed via the super admin UI. The encryption key itself lives only in the Next.js app's own VPS-local `.env`, never in git or the database. |
| `nginx/supabase.subtrakr.me.conf` | Let's Encrypt certs (`/etc/letsencrypt/`) |

The CD workflow (`.github/workflows/deploy-backend.yml`) copies the repo's
`backend/` files onto the VPS, runs `envsubst` against the template using the
VPS's own existing `.env` (which it never overwrites), applies `schema.sql`,
and restarts the stack — so secrets only ever exist on the VPS itself.

## First-time bootstrap (already done for this project)

If this stack is ever rebuilt from scratch, see
[[subtrakr_supabase_infra]] memory for the exact gotchas hit doing this the
first time (wrong image tags, `supabase_admin` vs `postgres` privilege
confusion, role passwords needing a manual fix, etc.) — the short version:

1. `mkdir -p /var/www/subtrakr-supabase`, write `.env` with fresh
   `POSTGRES_PASSWORD` / `JWT_SECRET`, and derive `ANON_KEY` /
   `SERVICE_ROLE_KEY` as HS256 JWTs signed with that secret
   (`{"role":"anon",...}` / `{"role":"service_role",...}`).
2. `docker compose up -d`, then connect as `supabase_admin` (the real
   superuser in the `supabase/postgres` image — `postgres` is deliberately
   not one) and `ALTER ROLE ... WITH PASSWORD` on `authenticator`,
   `supabase_auth_admin`, and `supabase_storage_admin` to match
   `POSTGRES_PASSWORD`.
3. Add the DNS A record, bootstrap an HTTP-only nginx config for the ACME
   challenge, `certbot certonly --webroot`, then swap in the full
   HTTPS config once the cert exists.
4. Run `schema.sql` against the database.

## Web build deploy

`.github/workflows/deploy-web.yml` builds `flutter build web --release` on
the same self-hosted runner and serves it at `app.subtrakr.me`, following the
same nginx-proxy + certbot pattern as the backend.
